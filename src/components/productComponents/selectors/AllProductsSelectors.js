export const getProductsIsDone = (state) => state.products.isDone;

export const getProductsFailure = (state) => state.products.failure;

export const getProducts = (state) => state.products.items;

export const getProductsLoading = (state) => state.products.loading;

export const getProductsError = (state) => state.products.error;
